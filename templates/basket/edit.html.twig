<!DOCTYPE html>

<title>Edit Basket</title>

{% block body %}
    <h1>Edit Basket</h1>

    {{ include('basket/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_basket_index') }}">back to list</a>

    {{ include('basket/_delete_form.html.twig') }}
{% endblock %}
