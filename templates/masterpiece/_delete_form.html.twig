<form method="post" action="{{ path('app_masterpiece_delete', {'id': masterpiece.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ masterpiece.id) }}">
    <button class="btn">Delete</button>
</form>
